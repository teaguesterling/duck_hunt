if [ "$1" == -v ] ; then 
    make 2>&1 | tee >(tee make.out 1>&2) | ./build/release/duckdb -s "INSTALL json; LOAD json; COPY ( SELECT * EXCLUDE (raw_output) FROM read_test_results('/dev/stdin', 'auto') ) TO '/dev/stdout' (FORMAT json);" | tee .build-logs/make-release-$(git rev-parse --short HEAD).json | jq . && ln -vsfn make-release-$(git rev-parse --short HEAD).json .build-logs/make-release-HEAD.json && make test 2>&1 | tee >(tee make-test.out 1>&2) |./build/release/duckdb -s "INSTALL json; LOAD json; COPY ( SELECT * EXCLUDE (raw_output) FROM read_test_results('/dev/stdin', 'auto') ) TO '/dev/stdout' (FORMAT json);" | tee .build-logs/make-test-$(git rev-parse --short HEAD).json | jq . 
else
    make 2>&1 | ./build/release/duckdb -s "INSTALL json; LOAD json; COPY ( SELECT * EXCLUDE (raw_output) FROM read_test_results('/dev/stdin', 'auto') ) TO '/dev/stdout' (FORMAT json);" | tee .build-logs/make-release-$(git rev-parse --short HEAD).json | jq . && ln -vsfn make-release-$(git rev-parse --short HEAD).json .build-logs/make-release-HEAD.json && make test 2>&1 |./build/release/duckdb -s "INSTALL json; LOAD json; COPY ( SELECT * FROM read_test_results('/dev/stdin', 'auto') ) TO '/dev/stdout' (FORMAT json);" | tee .build-logs/make-test-$(git rev-parse --short HEAD).json | jq . 
fi
